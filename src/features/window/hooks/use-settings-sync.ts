/**
 * Coodi is local-first and account-free. Cloud settings synchronization is
 * intentionally disabled; settings remain on this device.
 */
export function useSettingsSync(): void {
  // Deliberately empty for compatibility with the existing bootstrap hook.
}
